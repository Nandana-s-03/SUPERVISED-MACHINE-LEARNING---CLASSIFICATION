# WEATHER CLASSIFICATION USING KNN
# This project demonstrates how to classify weather conditions using the K-Nearest Neighbors (KNN) algorithm.
# The dataset contains categorical features such as outlook, temperature, humidity, and wind, with the target variable being the weather class (e.g., Sunny, Rainy, Cloudy).
# KNN is a simple yet powerful algorithm that classifies data points based on the majority class of their nearest neighbors.
# PROJECT WORKFLOW
# 1. Import Libraries
# 2. Load the dataset 
# 3. Data Preprocessing – Missing values checked, target column (Heart Disease) encoded using LabelEncoder
# 4. Feature Selection
# 5. Split the dataset into training and testing sets
# 6. Train Random Forest Classifier model
# 7. Predict the class labels on test data
# 8. Evaluate the model using:
#    Accuracy score
